# Contributing

Stub — populated once the project has an external contributor surface. For now: follow the phase plan in `ROADMAP.md`, keep scope tight (see `Kestrel_Project_Brief.pdf` §10), run `uv run pytest` before each change.
