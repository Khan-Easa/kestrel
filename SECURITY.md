# Security

Stub — populated alongside Phase 2 (Docker isolation) and Phase 7 (auth hardening).

**Phase 1 note:** the service executes arbitrary Python code in a subprocess on the host. Do **not** expose it to untrusted input outside a throwaway dev machine.
